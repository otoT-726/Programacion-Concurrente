sem espacioTotal = 7, espacioT = 5, espacioM = 5;

process CamionTrigo[id: 0..T-1]{
  P(espacioT);
  P(espacioTotal);
  //descargar
  V(espacioTotal);
  V(espacioT);
}

process CamionMaiz[id: 0..M-1]{
  P(espacioM);
  P(espacioTotal);
  //descargar
  V(espacioTotal);
  V(espacioM);
}
